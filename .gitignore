# Don't include .env files (we create .env-sample files instead)
*.env

# Also, don't include specific oauth_proxy authorized emails files
authenticated-emails.txt

# Logs and databases #
######################
*.log
*.sql
*.sqlite

# OS generated files #
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
acme.json
autopirate-fp.yml

# Don't check in manifests generated by helm local CI
*/manifests
.vscode

# Don't check in any file named "myvalues.yml" or "myvalues.yaml"
myvalues.yml
myvalues.yaml
